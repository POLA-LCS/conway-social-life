<?php

session_start();

define('ROOT_DIR', dirname(__DIR__) . '/');
define('CONFIG_DIR', ROOT_DIR . 'config/');

require CONFIG_DIR . 'paths.php';

// TODO: Add the routing and the action controller

exit();
